<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coursera.semana3.mascotas1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/pelota"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pelota"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mascotas1">
        <activity
            android:name=".MascotasFavoritas"
            android:label="@string/favoritos"
            android:parentActivityName=".MainActivity">
            <!--soporte para versiones android 4.0 o inferior-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"
                ></meta-data>

        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>